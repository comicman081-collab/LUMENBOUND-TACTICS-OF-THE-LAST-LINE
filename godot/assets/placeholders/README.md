# DEV placeholders

`code_native/dev_placeholder.svg` is an original code-native fallback used so data-level `asset_id` references always resolve. It is not final character, story, UI, or marketing art.
