{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Lanternline ScenarioDef",
  "type": "object",
  "required": ["id", "title_key", "chapter_id", "commands"],
  "properties": {
    "id": {"type": "string", "minLength": 1},
    "commands": {"type": "array", "minItems": 1, "items": {"type": "object", "required": ["command"]}}
  }
}

