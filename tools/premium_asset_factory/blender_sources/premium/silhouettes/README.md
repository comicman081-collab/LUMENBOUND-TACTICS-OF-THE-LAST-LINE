# Premium pilot Blender sources

`pilot_silhouettes.blend` is generated deterministically by `build_premium_pilot.py`.
The generated `.blend` is a build artifact and remains `PREMIUM_PILOT` until visual QA.
